

Gets an array list of resource IDs that have the files with the images

When you create a new image adapter you have to pass in the ids

and you save the ids in a local field in the adapter so the adapter can remmber the resourceIDs of those values.