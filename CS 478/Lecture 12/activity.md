
An activity in android is:

A single focused thing that a user can do. Almost all activities interact with the user, so the Activity class takes care of creating a window for you in which you can place your UI with the `setContentView(view)`

QUICK **REFERENCE**
https://developer.android.com/reference/android/app/Activity