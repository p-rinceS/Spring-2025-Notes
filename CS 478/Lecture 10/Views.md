



Each View has to know how to draw itself.


### Three Stage Process
(Callbacks)

<font color="#f79646">onMeasure() </font>
<font color="#f79646">onLayout()</font>
<font color="#f79646">onDraw()</font>


### Additional View APIs

- Visibility (getVisiblity(), setVisibility()
- Selected state (isSelected(), setSelected())
- Clickable (isClickable(), setClickable())
- Focus (hasFocus(), requestFocus(), setFocusable())
	- ... and more..