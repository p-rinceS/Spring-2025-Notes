 "lock" is a general concept representing a mechanism to control access to a shared resource, ensuring only one thread can modify it at a time

From my understanding and my understanding was wrong, I am confusing it with [Mutex]([[Mutual Exclusion]]).

It is a piece of code that will *"lock"* a block of code from being accessed by other threads until the "*lock*" is "*unlocked*". 